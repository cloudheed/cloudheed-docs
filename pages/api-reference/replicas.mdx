# Replica Endpoints

API reference for read replica endpoints.

import { Callout } from 'nextra/components'

## Create Replica

Create a read replica for a database.

<span className="method-badge method-post">POST</span> `/v1/databases/{id}/replicas`

### Request Body

```json
{
  "region": "fsn1",
  "name": "my-database-replica-1"
}
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `region` | string | No | Region for replica (defaults to primary region) |
| `name` | string | No | Replica name (auto-generated if empty) |

### Response `202 Accepted`

```json
{
  "replica": {
    "id": "ch-replica-fsn1-abc123",
    "instance_id": "db-abc123",
    "name": "my-database-replica-1",
    "region": "fsn1",
    "status": "pending",
    "replication_state": "disconnected",
    "replication_lag_bytes": 0,
    "replication_lag_seconds": 0,
    "created_at": "2026-03-17T10:00:00Z"
  },
  "message_code": "replica_creation_started"
}
```

<Callout type="info">
  Replicas are not available on the Free tier. Maximum 2 replicas per database.
</Callout>

---

## List Replicas

<span className="method-badge method-get">GET</span> `/v1/databases/{id}/replicas`

### Response

```json
{
  "replicas": [
    {
      "id": "ch-replica-fsn1-abc123",
      "instance_id": "db-abc123",
      "name": "my-database-replica-1",
      "region": "fsn1",
      "status": "running",
      "host": "ch-replica-fsn1-abc123.db.cloudheed.cloud",
      "port": 5432,
      "connection_url": "postgresql://cloudheed:****@ch-replica-fsn1-abc123.db.cloudheed.cloud:5432/postgres?sslmode=require",
      "replication_state": "streaming",
      "replication_lag_bytes": 0,
      "replication_lag_seconds": 0,
      "created_at": "2026-03-17T10:00:00Z"
    }
  ]
}
```

---

## Get Replica

<span className="method-badge method-get">GET</span> `/v1/databases/{id}/replicas/{replicaId}`

### Response

```json
{
  "id": "ch-replica-fsn1-abc123",
  "instance_id": "db-abc123",
  "name": "my-database-replica-1",
  "region": "fsn1",
  "status": "running",
  "host": "ch-replica-fsn1-abc123.db.cloudheed.cloud",
  "port": 5432,
  "connection_url": "postgresql://cloudheed:****@ch-replica-fsn1-abc123.db.cloudheed.cloud:5432/postgres?sslmode=require",
  "replication_state": "streaming",
  "replication_lag_bytes": 0,
  "replication_lag_seconds": 0,
  "created_at": "2026-03-17T10:00:00Z"
}
```

### Replica Status

| Status | Description |
|--------|-------------|
| `pending` | Replica creation queued |
| `provisioning` | Replica being created |
| `syncing` | Initial data sync in progress |
| `running` | Replica active and streaming |
| `failed` | Replica creation failed |
| `stopped` | Replica stopped |

### Replication State

| State | Description |
|-------|-------------|
| `disconnected` | Not connected to primary |
| `streaming` | Actively replicating changes |
| `catchup` | Catching up to primary |

---

## Delete Replica

<span className="method-badge method-delete">DELETE</span> `/v1/databases/{id}/replicas/{replicaId}`

### Response `202 Accepted`

```json
{
  "message_code": "replica_deletion_started"
}
```
